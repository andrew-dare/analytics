# Copy to .env and fill in real values. .env is gitignored.

# From https://dashboard.clerk.com/~/api-keys — safe to expose to the
# browser (it's a publishable key, not the secret key).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_with_real_key
